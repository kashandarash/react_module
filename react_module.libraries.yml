app:
  js:
    js/dist/main.js: {}
  css:
    component:
      # React component above could have own styles, but for couple lines let's simply use separate file.
      css/main.css: {}
  dependencies:
    - core/drupalSettings
