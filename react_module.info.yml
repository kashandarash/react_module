name: React module
type: module
description: Provides a Drupal module with a block. The block contains a React library with a TODO list that communicates with the site owner's GraphQL.
package: Custom
core_version_requirement: ^10
dependencies:
  - graphql:graphql (^4.8)
